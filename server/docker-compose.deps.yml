version: '3.8'

services:
  # Local Redis for Development
  redis:
    image: redis:alpine
    container_name: erp_redis_dev
    ports:
      - "6379:6379"
    restart: always
  # Local Postgres (Optional, if they want to run DB locally in docker too)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: erp_postgres_dev
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: rqi_erp
